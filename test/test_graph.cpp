#include <gtest.h>
#include "dijkstra.h"
#include "dheap.h"

TEST(Graph, null_ptr) {
	ASSERT_EQ(1, 1);
}

TEST(Graph, null_sz) {
	ASSERT_EQ(1, 1);
}

TEST(Graph, copy_cst) {
	ASSERT_EQ(1, 1);
}
TEST(Graph, table_size) {
	ASSERT_EQ(1, 1);
}
TEST(Graph, row_size) {
	ASSERT_EQ(1, 1);
}

TEST(Graph, rand_gen) {
	ASSERT_EQ(1, 1);
}
TEST(Graph, input_can) {
	ASSERT_EQ(1, 1);
}